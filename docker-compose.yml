services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: virginia
      MYSQL_DATABASE: virginia_spa
      MYSQL_USER: virginia
      MYSQL_PASSWORD: virginia
    command: ["--character-set-server=utf8mb4", "--collation-server=utf8mb4_unicode_ci"]
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-u", "virginia", "-pvirginia"]
      interval: 5s
      timeout: 5s
      retries: 20
